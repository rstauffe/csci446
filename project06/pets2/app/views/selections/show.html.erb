<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Pets for Consideration</h2>
<ul>
	<% @selection.adopted_pets.each do |pet| %>
		<li> <%= pet.cat.name %></li>
	<% end %>
</ul>
<%= button_to 'Remove Selections', @selection, method: :delete, data: {confirm: 'Are you sure?' } %>